/**
 * The gltf package contains all classes necessary to load the gltf structure and binary data referenced by it into the memory.
 *
 * <h3>Usage</h3>
 * To load a gltf file into memory simply call the constructor of {@link gltf.GLTFAsset} with the file you want to load.
 * It will do it's best effort to load everything into memory for later use.
 */
package gltf;